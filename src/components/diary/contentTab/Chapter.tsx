import React, {memo} from 'react';
import {View} from 'react-native';

type TProps = {
   number: number
   name: string
}
export const Chapter = memo((props: TProps) => {
  const {} = props;

  return (
    <View>

    </View>
  );
});
